#include <bits/stdc++.h>
using namespace std;

int main() {
    int W, H;
    cin >> W >> H;
    if (W >= 60 && H <= 130) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
